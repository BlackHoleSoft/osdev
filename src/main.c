#include <stdio.h>

int main() {
    printf("Strelka system\n");
    printf("Hello from C with static stdlib");

    char buff[10];
    int i = 0;


    while (i < 10 || buff[i] = scanf("%c", &buf[i]) != '\n') {
        printf("%c", buf[i]);
    }

    printf("%10s", buff);

    while(1) {
        // loop
    }

    return 0;
}